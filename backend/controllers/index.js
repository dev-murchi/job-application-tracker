const authController = require('./auth');
const jobsController = require('./jobs');
const userController = require('./user');

module.exports = {
  authController,
  jobsController,
  userController,
};
