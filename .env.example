# Development Environment Configuration
# Usage: docker compose --profile [profile] --env-file .env.file up
# profiles: [dev, production, backend-test, frontend-test]

# ===== Application Configuration =====
NODE_ENV=example    # values: [dev, test, production]
HOST_PORT=3000      # For development
SERVER_PORT=3000
FRONTEND_PORT=4200  # For development and tests

MONGO_INITDB_ROOT_USERNAME=user
MONGO_INITDB_ROOT_PASSWORD=password
MONGO_URL=mongodb://user:password@mongodb:27017/example_database?authSource=admin

# JWT Configuration
# Generate: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_LIFETIME=30d

# CORS Configuration
# Development: http://localhost:4200
# Production: https://yourdomain.com
CORS_ORIGIN=http://localhost:4200

# Optional: API Rate Limiting (requests per 15 minutes)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Optional: Logging Level (error, warn, info, http, debug)
LOG_LEVEL=info

# Request size limit for body parser
REQUEST_SIZE_LIMIT=100kb

# ===== Nginx (not used in dev profile) =====
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
